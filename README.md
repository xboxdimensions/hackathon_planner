"# hackathon_planner" 
WHy

# Initialize and Running Flask Project
$ python3 -m venv .venv
$ . .venv/bin/activate
Within this environment:
$ pip install Flask

# RUNNING FLASK PROJECT
. .venv/bin/activate
flask run